<Window x:Class="VDrumExplorer.Guid.Dialogs.DataTransferDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vmd="clr-namespace:VDrumExplorer.ViewModel.Dialogs;assembly=VDrumExplorer.ViewModel"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="140" Width="300"
        d:DataContext="{d:DesignInstance Type=vmd:DataTransferViewModel}"
        Style="{StaticResource WindowStyle}">
    <StackPanel>
        <TextBlock Text="{Binding CurrentItem}" />
        <Grid Margin="5,8,5,0" Height="23">
            <ProgressBar Value="{Binding Completed}" Maximum="{Binding Total}" />
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{} {0} / {1}">
                        <Binding Path="Completed" />
                        <Binding Path="Total" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </Grid>
        <Button Padding="5,2" Margin="5,8,5,8" Click="Cancel" HorizontalAlignment="Left">Cancel</Button>
    </StackPanel>
</Window>
